"""
Test models.

Tests will run with pytest when pushed to remote, and can also be run manually
with:
        $ pytest tests/test_models.py

TODO: Add tests and test data for each model
"""
import pytest
import torch




